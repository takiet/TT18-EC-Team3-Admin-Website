export enum EventListener {
  click = 'click',
  touchend = 'touchend',
  scroll = 'scroll',
  input = 'input',
  pause = 'pause',
  play = 'play',
}

export enum EToken {
  loginToken = 'localStorage@access',
}

